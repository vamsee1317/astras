// Component :

    // Functional Components are nothing js functions which will optionally accept props & states as an input and returns jsx as an output

    // Class Components are nothing but js classes which will optionally accept props & states as an input and returns jsx as an output
